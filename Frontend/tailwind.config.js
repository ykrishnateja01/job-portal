/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: 'class', // Enables toggling via `class="dark"`
  content: ['./src/**/*.{js,jsx,ts,tsx}'], // Adjust to your src paths
  theme: {
    extend: {},
  },
  plugins: [],
};
